# Scripts

Automation scripts will live here once the labs are implemented.

Planned scripts:

- `deploy-lab.sh`
- `run-failure-test.sh`
- `capture-evidence.sh`
- `destroy-lab.sh`

Scripts should be repeatable and should print the evidence locations they create.

Repository convention: use Git Bash compatible `.sh` scripts only. Do not add PowerShell scripts.
